{
  "name": "adaptive-maze-td",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "sim": "esbuild src/sim/run.ts --bundle --platform=node --format=esm --outfile=sim/_run.mjs && node sim/_run.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vite": "^5.4.2"
  }
}
